from setuptools import setup

setup(name='bandu',
      version='0.1',
      description='Bandu + ipdf minimal codebase',
      packages=['bandu', 'supervised_training', 'helper_scripts', 'ipdf'],
      zip_safe=False)
